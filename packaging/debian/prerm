#!/bin/sh
set -eu

case "$1" in
    remove|deconfigure)
        if [ -d /run/systemd/system ]; then
            systemctl disable --now cockpit-snapraid-api-guard.service || true
        fi
        ;;
esac

exit 0
